# System Architecture - Sponsor a Child

## 🏗️ Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (Next.js)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐         ┌──────────────────┐             │
│  │   Public Pages   │         │   Admin Pages    │             │
│  ├──────────────────┤         ├──────────────────┤             │
│  │                  │         │                  │             │
│  │  /sponsor        │         │  /admin          │             │
│  │  - View children │         │  - Login         │             │
│  │  - Filter        │         │                  │             │
│  │  - Sponsor btn   │         │  /admin/dash     │             │
│  │                  │         │  - Add child     │             │
│  │  /               │         │  - Edit child    │             │
│  │  - Homepage      │         │  - Delete child  │             │
│  │  - Hero          │         │  - View all      │             │
│  │                  │         │                  │             │
│  └────────┬─────────┘         └────────┬─────────┘             │
│           │                            │                        │
└───────────┼────────────────────────────┼────────────────────────┘
            │                            │
            │                            │
            ↓                            ↓
┌───────────────────────────────────────────────────────────────┐
│                      API ROUTES (Next.js)                      │
├───────────────────────────────────────────────────────────────┤
│                                                                 │
│  GET  /api/children              → Fetch all children          │
│  POST /api/children              → Create child (admin)        │
│  GET  /api/children/[id]         → Fetch one child            │
│  PUT  /api/children/[id]         → Update child (admin)       │
│  DELETE /api/children/[id]       → Delete child (admin)       │
│  POST /api/donorbox-webhook      → Receive payments           │
│                                                                 │
└────────┬──────────────────────────────────────────┬───────────┘
         │                                           │
         │                                           │
         ↓                                           ↓
┌─────────────────────┐                   ┌──────────────────┐
│   SUPABASE          │                   │   DONORBOX       │
│   (Database)        │                   │   (Payments)     │
├─────────────────────┤                   ├──────────────────┤
│                     │                   │                  │
│ Tables:             │                   │ - Campaign       │
│ • children          │                   │ - Donation form  │
│ • sponsorships      │                   │ - Webhooks       │
│                     │                   │ - Receipts       │
│ RLS Policies:       │                   │                  │
│ • Public read       │◄──────────────────┤ Webhook sends    │
│ • Service write     │                   │ donation data    │
│                     │                   │                  │
└─────────────────────┘                   └──────────────────┘
```

---

## 🔄 User Flow Diagrams

### **Public User Flow (Sponsor a Child)**

```
┌─────────────┐
│ User visits │
│  /sponsor   │
└──────┬──────┘
       │
       ↓
┌─────────────────────┐
│ View grid of        │
│ available children  │
│ (from Supabase)     │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ Click on child card │
│ to read full bio    │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ Click "Sponsor"     │
│ button              │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ DonorBox popup      │
│ opens with:         │
│ - Child name        │
│ - Amount needed     │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ User fills form:    │
│ - Name              │
│ - Email             │
│ - Card details      │
│ - Amount            │
│ - Frequency         │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ Payment processed   │
│ by DonorBox         │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ DonorBox sends      │
│ webhook to:         │
│ /api/donorbox-      │
│ webhook             │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ API creates         │
│ sponsorship record  │
│ in Supabase         │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ API updates child:  │
│ - amount_raised ↑   │
│ - status updates    │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ Progress bar on     │
│ /sponsor updates    │
│ automatically       │
└─────────────────────┘
```

### **Admin Flow (Manage Children)**

```
┌─────────────┐
│ Admin goes  │
│ to /admin   │
└──────┬──────┘
       │
       ↓
┌─────────────────────┐
│ Enter password      │
│ (from .env.local)   │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ Login successful    │
│ → /admin/dashboard  │
└──────┬──────────────┘
       │
       ↓
┌─────────────────────┐
│ See table of all    │
│ children with:      │
│ - Photo             │
│ - Name              │
│ - Age               │
│ - Class             │
│ - Amounts           │
│ - Status badge      │
│ - Edit/Delete btns  │
└──────┬──────────────┘
       │
       ├──────────────────┬──────────────┬──────────────┐
       │                  │              │              │
       ↓                  ↓              ↓              ↓
┌─────────────┐  ┌─────────────┐  ┌──────────┐  ┌──────────┐
│ Click       │  │ Click       │  │ Click    │  │ View all │
│ Add Child   │  │ Edit        │  │ Delete   │  │ children │
└──────┬──────┘  └──────┬──────┘  └────┬─────┘  └──────────┘
       │                │              │
       ↓                ↓              ↓
┌─────────────┐  ┌─────────────┐  ┌──────────┐
│ Fill form:  │  │ Update form │  │ Confirm  │
│ - Name      │  │ - Change    │  │ deletion │
│ - Age       │  │   details   │  └────┬─────┘
│ - Class     │  └──────┬──────┘       │
│ - Bio       │         │              │
│ - Amount    │         ↓              ↓
│ - Photo URL │  ┌─────────────┐  ┌──────────┐
└──────┬──────┘  │ PUT request │  │ DELETE   │
       │         │ to API      │  │ request  │
       ↓         └──────┬──────┘  │ to API   │
┌─────────────┐         │         └────┬─────┘
│ POST req to │         │              │
│ API         │         │              │
└──────┬──────┘         │              │
       │                │              │
       └────────────────┼──────────────┘
                        │
                        ↓
                ┌──────────────┐
                │ Supabase DB  │
                │ updated      │
                └──────┬───────┘
                       │
                       ↓
                ┌──────────────┐
                │ Table        │
                │ refreshes    │
                └──────────────┘
```

---

## 📦 Component Structure

```
src/
├── app/
│   ├── layout.tsx                    # Root layout
│   ├── page.tsx                      # Homepage
│   │
│   ├── admin/
│   │   ├── page.tsx                  # Login page
│   │   │   ├── Password input
│   │   │   ├── Submit button
│   │   │   └── Validation
│   │   │
│   │   └── dashboard/
│   │       └── page.tsx              # Admin dashboard
│   │           ├── Header (logout)
│   │           ├── Add button
│   │           ├── Form (add/edit)
│   │           └── Children table
│   │
│   ├── sponsor/
│   │   └── page.tsx                  # Public sponsor page
│   │       ├── Header component
│   │       ├── Hero section
│   │       ├── Filter buttons
│   │       ├── Children grid
│   │       │   ├── Child card
│   │       │   │   ├── Photo
│   │       │   │   ├── Name/details
│   │       │   │   ├── Bio
│   │       │   │   ├── Progress bar
│   │       │   │   └── Sponsor button
│   │       │   └── ... (repeated)
│   │       ├── How it works section
│   │       └── Footer component
│   │
│   └── api/
│       ├── children/
│       │   ├── route.ts              # GET all, POST new
│       │   └── [id]/
│       │       └── route.ts          # GET, PUT, DELETE one
│       │
│       └── donorbox-webhook/
│           └── route.ts              # POST webhook handler
│
├── components/
│   ├── Header.tsx                    # Site header
│   ├── Footer.tsx                    # Site footer
│   └── Hero.tsx                      # Homepage hero
│
├── lib/
│   └── supabase/
│       ├── client.ts                 # Browser client
│       └── server.ts                 # Server client
│
└── types/
    └── database.types.ts             # TypeScript types
```

---

## 🗃️ Database Entity Relationship

```
┌─────────────────────────────────────────┐
│              children                    │
├─────────────────────────────────────────┤
│ PK  id (uuid)                           │
│     name (text)                          │
│     bio (text)                           │
│     class_year (text)                    │
│     age (integer)                        │
│     location (text)                      │
│     interests (text)                     │
│     amount_needed (numeric)              │
│     amount_raised (numeric)              │
│     photo_url (text)                     │
│     status (enum)                        │
│     created_at (timestamp)               │
│     updated_at (timestamp)               │
└────────────┬────────────────────────────┘
             │
             │ 1:N (one child → many sponsorships)
             │
             ↓
┌─────────────────────────────────────────┐
│           sponsorships                   │
├─────────────────────────────────────────┤
│ PK  id (uuid)                           │
│ FK  child_id (uuid) → children.id       │
│     donor_name (text)                    │
│     donor_email (text)                   │
│     amount (numeric)                     │
│     frequency (enum)                     │
│     donorbox_transaction_id (text)       │
│     status (enum)                        │
│     created_at (timestamp)               │
└─────────────────────────────────────────┘
```

**Relationship**: One child can have many sponsorships.  
**Cascade**: Deleting a child deletes all their sponsorships.

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────┐
│                  Public Users                    │
│              (No authentication)                 │
└────────────┬────────────────────────────────────┘
             │
             ↓ READ-ONLY ACCESS
┌─────────────────────────────────────────────────┐
│              Supabase RLS Policies               │
│  ┌───────────────────────────────────────────┐  │
│  │ SELECT on children: ✅ Public allowed     │  │
│  │ INSERT on children: ❌ Blocked            │  │
│  │ UPDATE on children: ❌ Blocked            │  │
│  │ DELETE on children: ❌ Blocked            │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│                  Admin Users                     │
│           (Password authenticated)               │
└────────────┬────────────────────────────────────┘
             │
             ↓ WITH x-admin-password HEADER
┌─────────────────────────────────────────────────┐
│                API Route Guards                  │
│  ┌───────────────────────────────────────────┐  │
│  │ if (password !== ADMIN_PASSWORD)          │  │
│  │   return 401 Unauthorized                 │  │
│  └───────────────────────────────────────────┘  │
└────────────┬────────────────────────────────────┘
             │
             ↓ WRITE ACCESS VIA SERVICE ROLE
┌─────────────────────────────────────────────────┐
│            Supabase Service Client               │
│         (Bypasses RLS with service key)          │
│  ┌───────────────────────────────────────────┐  │
│  │ INSERT, UPDATE, DELETE allowed            │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│                  DonorBox                        │
│            (Webhook authenticated)               │
└────────────┬────────────────────────────────────┘
             │
             ↓ WITH x-donorbox-signature HEADER
┌─────────────────────────────────────────────────┐
│            Webhook Verification                  │
│  ┌───────────────────────────────────────────┐  │
│  │ if (signature !== WEBHOOK_SECRET)         │  │
│  │   return 401 Unauthorized                 │  │
│  └───────────────────────────────────────────┘  │
└────────────┬────────────────────────────────────┘
             │
             ↓ CREATE SPONSORSHIP & UPDATE CHILD
┌─────────────────────────────────────────────────┐
│            Supabase Service Client               │
└─────────────────────────────────────────────────┘
```

---

## 🌊 Data Flow Sequences

### **Sequence 1: Admin Adds Child**

```
Admin → Admin Page → API Route → Supabase → Public Page

1. Admin fills form on /admin/dashboard
2. Form submitted with admin password
3. POST /api/children
4. API validates password
5. API inserts into Supabase (service role)
6. Supabase returns new child
7. Admin dashboard refreshes
8. Child now visible on /sponsor
```

### **Sequence 2: User Sponsors Child**

```
User → Sponsor Page → DonorBox → Webhook → API → Supabase → Sponsor Page

1. User clicks "Sponsor" on /sponsor
2. DonorBox popup opens
3. User completes payment
4. DonorBox processes payment
5. DonorBox sends webhook
6. POST /api/donorbox-webhook
7. API validates webhook signature
8. API creates sponsorship record
9. API updates child.amount_raised
10. API updates child.status (if needed)
11. /sponsor page auto-refreshes
12. Progress bar updates
```

---

## 🚦 Status Flow

```
Child Status Lifecycle:

┌─────────────┐
│  available  │ (amount_raised = 0)
└──────┬──────┘
       │
       │ First sponsorship received
       ↓
┌──────────────────────┐
│ partially_sponsored  │ (0 < amount_raised < amount_needed)
└──────┬───────────────┘
       │
       │ Amount raised >= Amount needed
       ↓
┌──────────────────┐
│ fully_sponsored  │ (amount_raised >= amount_needed)
└──────────────────┘
       │
       │ Hidden from public /sponsor page
       ↓
┌──────────────────┐
│ Still in admin   │
│ dashboard        │
└──────────────────┘
```

---

## 📱 Responsive Design Breakpoints

```
Mobile (< 640px)
┌─────────────┐
│ ┌─────────┐ │
│ │ Child 1 │ │
│ └─────────┘ │
│ ┌─────────┐ │
│ │ Child 2 │ │
│ └─────────┘ │
│ ┌─────────┐ │
│ │ Child 3 │ │
│ └─────────┘ │
└─────────────┘
(1 column grid)

Tablet (640px - 1024px)
┌──────────────────────┐
│ ┌───────┐ ┌───────┐ │
│ │Child 1│ │Child 2│ │
│ └───────┘ └───────┘ │
│ ┌───────┐ ┌───────┐ │
│ │Child 3│ │Child 4│ │
│ └───────┘ └───────┘ │
└──────────────────────┘
(2 column grid)

Desktop (> 1024px)
┌───────────────────────────────┐
│ ┌─────┐ ┌─────┐ ┌─────┐      │
│ │Ch 1 │ │Ch 2 │ │Ch 3 │      │
│ └─────┘ └─────┘ └─────┘      │
│ ┌─────┐ ┌─────┐ ┌─────┐      │
│ │Ch 4 │ │Ch 5 │ │Ch 6 │      │
│ └─────┘ └─────┘ └─────┘      │
└───────────────────────────────┘
(3 column grid)
```

---

## 🎨 Styling Architecture

```
Tailwind CSS Utility Classes
    ↓
Custom School Colors
    ├── school-green: #2E8B57
    ├── school-blue: #000080
    ├── school-yellow: #F4D03F
    └── school-red: #E74C3C
        ↓
Custom Fonts
    ├── font-heading: Crimson Pro
    └── font-paragraph: Inter
        ↓
Responsive Design
    ├── Mobile-first approach
    ├── Breakpoints: sm, md, lg, xl
    └── Flexible grids
        ↓
Components
    ├── Buttons (hover states)
    ├── Cards (shadow on hover)
    ├── Forms (focus states)
    └── Tables (zebra striping)
```

---

## 🧩 Technology Stack Summary

```
┌────────────────────────────────────┐
│         Frontend Layer             │
├────────────────────────────────────┤
│ • Next.js 15 (App Router)          │
│ • React 18                          │
│ • TypeScript                        │
│ • Tailwind CSS                      │
└──────────────┬─────────────────────┘
               │
┌──────────────┴─────────────────────┐
│           API Layer                │
├────────────────────────────────────┤
│ • Next.js API Routes               │
│ • RESTful endpoints                │
│ • Server-side rendering            │
└──────────────┬─────────────────────┘
               │
┌──────────────┴─────────────────────┐
│         Database Layer             │
├────────────────────────────────────┤
│ • Supabase (PostgreSQL)            │
│ • Row Level Security (RLS)         │
│ • Real-time subscriptions          │
└──────────────┬─────────────────────┘
               │
┌──────────────┴─────────────────────┐
│        External Services           │
├────────────────────────────────────┤
│ • DonorBox (Payments)              │
│ • Webhooks (Notifications)         │
└────────────────────────────────────┘
```

---

This architecture provides a **scalable**, **secure**, and **maintainable** solution for managing child sponsorships! 🏗️

